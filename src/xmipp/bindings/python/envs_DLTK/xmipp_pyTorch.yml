#Protocols ussing the enviroment: protocol_deepHand, classify_pca

name: xmipp_pyTorch
channels:
  - conda-forge
dependencies:
  - python=3.10
  - numpy=1.23
  - mrcfile=1.4.3
  - kornia=0.6.8  # Última con formato *.bz2
  - starfile=0.4.11  # Última con formato *.bz2
  - pip
  - pip:
      - torch==1.13.0
      - torchvision==0.14.0